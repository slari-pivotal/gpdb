platform: linux
image_resource:
  type: docker-image
  source:
    repository: pivotaldata/centos511-java7-gpdb-dev-image
    username: gpdb
    password: one team one dream
    email: releng@pivotal.io
inputs:
  - name: gpdb_src
  - name: bin_gpdb
outputs:
params:
  BEHAVE_TAGS: ""
run:
  path: gpdb_src/ci/concourse/scripts/behave_tests.bash
