platform: linux
inputs:
  - name: gpdb_src
outputs:
  - name: gpdb_src_archive
run:
  path: gpdb_src/ci/concourse/scripts/gpdb_src_packaging.bash
params:
  GPDB_SRC_TAR_GZ: gpdb_src_archive/greenplum-db-VERSION_OS-x86_64_src.tar.gz
