platform: linux
image_resource:
  type: docker-image
inputs:
  - name: gpdb_src
  - name: bin_gpdb
  - name: pgcrypto_gppkg
outputs:
params:
  TEST_OS: "centos"
run:
  path: gpdb_src/ci/concourse/scripts/test_pgcrypto.bash
