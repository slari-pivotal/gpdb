---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
- name: gpdb_src

outputs:
- name: ccache_tar

run:
  path: gpdb_src/ci/concourse/scripts/bootstrap-ccache.sh
