platform: linux
image_resource:
  type: docker-image
  source:
    repository: pivotaldata/centos511-java7-gpdb-dev-image
    username: gpdb
    password: one team one dream
    email: releng@pivotal.io
inputs:
  - name: gpdb_src
  - name: bin_gpdb
outputs:
  - name: packaged_gpdb
run:
  path: gpdb_src/ci/concourse/scripts/gpdb_packaging.bash
params:
  INSTALL_SCRIPT_SRC: gpdb_src/gpAux/releng/bin/installer-header-foraspecificOS.sh
  GPDB_TARGZ: bin_gpdb/bin_gpdb.tar.gz
  INSTALLER_ZIP: packaged_gpdb/greenplum-db-VERSION-OS-x86_64.zip
